# Install packages

```
pip install -r requirements.txt
```

# Run the project

```
// Default port: 8000
uvicorn app.main:app --reload [--port <YOUR_PORT>]
```

# API docs

In the url: http://localhost:[PORT]/docs
